version: '3'
services:
  builder:
    build: .
    volumes:
      - ./bin:/home/app/bin
    environment:
      - TAG=0.0.0
      - CC=x86_64-w64-mingw32-gcc
      - OS=windows
      - ARCH=amd64
      - CGO_ENABLED=1
      - EXTENSION=.exe